// Disable CHLAUTH for development
ALTER QMGR CHLAUTH(DISABLED)

// Allow the app user to connect via DEV.ADMIN.SVRCONN
SET CHLAUTH(DEV.ADMIN.SVRCONN) TYPE(USERMAP) CLNTUSER('app') USERSRC(CHANNEL) ACTION(ADD)
